// ... (mantém o resto do script, só muda as mensagens pra combinar)

userGreeting.innerText = `Bem-vindo ao Calgarotto Finance, ${data.nome}!`;

showMsg(messageDashboard, "Depósito realizado no Calgarotto Finance!");